# Prompt — 2026-05-08T02:52:17Z

## User instruction (verbatim)

> So far, the skill and repository iteration has been focused on targeted object prompts ... The next important inference step is to you to determine what visualization objects and dashboard layout will be best to accomplish the users ask. ... Focus on keeping the dashboard simple so you can accurately produce results. Remember, you should bring all columns available in data model tables into the workbook for best calculation flexibility.
>
> okay, use the Plugs-Example-Data-Model data model and transaction details table to build a new workbook.
>
> I want a customer performance analysis. I want to understand how my customers are buying across stores, and what products are most popular. give me a dashboard that shows these details.
>
> [follow-up] please bring customer location demographics into the analysis as well by joining the customer details table to the transaction table. Revise your dashboard plan, additionally actually think about what filters would be best for the user to access in this scenario.
>
> [follow-up] on decision 1, Use approach B and use the actual workbook table element as the source of the visualization elements. you should be able to use the workbook table identifier as the source for visualizations. Perform the look up to get cust region.
>
> on decision 2, use the filters selected and the dashboard layout is approved

## Inference

**Data reality.** Plugs Transaction Details - Relationships (id `aFkvPq8xkX`) is a
warehouse-table join (LAB_DATA ⟕ D_PRODUCT on Product Name) with 24 columns
including `Cust Key` and `Customer Name`. Data model also exposes a separate
`Customer Details` element (id `6gvEU5lR39`) with rich location/demographic
fields. Available metrics on the transaction element: `Revenue`, `COGS`,
`Profit Margin`.

**Join decision (Decision 1 = approach B).** Pull Customer Details into the
workbook as a sibling table element; bring its columns into the transaction
table via `Lookup([Customer Details/<Col>], [Cust Key], [Customer Details/Cust Key])`.
All visualization elements source from the workbook table `tbl-plugs-tx`,
which now carries both transaction columns and looked-up customer columns. No
data-model modification.

**Filters (Decision 2 — approved).**

- Date range
- Cust Region (NEW location demographic, from Lookup)
- Loyalty Program (NEW, from Lookup — highest-signal customer attribute)
- Store Region (where the purchase happened)
- Product Family (broadest product slicer)

**Layout.** One page, single overview:

- Header container: title + 5 controls.
- KPI container: Total Customers (CountDistinct Cust Key), Total Revenue
  ([Metrics/Revenue]), Total Items Sold (Sum Quantity) — each with month
  date-dim for sparkline.
- Charts container (2×2 grid of bar charts):
  - A1 Revenue by Cust Region
  - A2 Revenue by Store Name
  - B1 Customers by Revenue (descending)
  - B2 Products by Quantity (descending)
- Detail table: full Plugs Transaction Details with all 23 transaction
  columns + 17 Customer Details Lookup columns + Revenue calc.
- Customer Details passthrough table: placed at bottom (must exist on the
  page so Lookup() has a sibling to resolve against; not the focus of the
  view).
